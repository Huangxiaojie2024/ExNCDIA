# Python dependencies for the NCDIA Risk Predictor (Streamlit app)
#
# IMPORTANT: replace the rdkit version below with the SAME version you used
# to compute the descriptors / fingerprints in your training .xlsx files.
# Check it in your training environment with:
#     python -c "import rdkit; print(rdkit.__version__)"

streamlit
rdkit==2022.9.5
scikit-learn==1.5.2
imbalanced-learn==0.12.3
numpy==1.26.4
pandas
scipy
joblib
